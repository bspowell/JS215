// INPUT
  // array of strings - assigned to parameter 'arr'
  // integer - assigned to paramter 'k'

// OUTPUT
  // return the 'k'th distinct string present in the array 'arr'
  // if few than 'k' distinct strings, return empty string

// RULES
  // a distinct string, is a string that present only once in an array
  // iterate over the full array before returning distinct string
  // 

// EXAMPLES / TEST CASES
  // ["d","b","c","b","c","a"], 2); // "a"
    // we are looking for the 2nd distinct string
    // 'd' - only 1 in array, first distinct string
    // 'b' - two in array, not distinct
    // 'c' - two in array, not distinct
    // 'a' - only 1, 2nd distinct

// Questions
  // Can the array contain other values than alphabetical? (Nan, Numbers, undefined, infinity, null)
    // if so, how should the program handle this input and return?
  // Can the array be sparse? ([1, , 2, 3, , , 4])
    // if so, how should the program handle this input and return?
  // Can the array contain uppercase and lowercase? 
    // if so, how should the program hanlde this and return? 
  // Can the array be empty? 
    // if so, how should the program hanlde this and what should it return? 
  // Can the integer be negative? 
  // Can the integer be another value besides integer?
  // 

// ALGO

// CODE

